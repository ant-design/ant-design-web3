/** 默认代币精度, 默认是ETH的换算成wei的精度18 */
export const TOKEN_DECIMALS_DEFAULT = 18;
/** 输入小数，允许六位小数，后面的截断, 产品给出的原因：如果值很小取不出来，没有意义 */
export const AMOUNT_IN_DECIMALS = 6;

/** 自定义前缀类名 */
export const CUSTOMIZE_PREFIX_CLS = 'ant-web3-swap-token';
